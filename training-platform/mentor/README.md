# Mentor-only controls

These scripts inject and clear deterministic simulator faults. Keep this directory restricted. Always verify a clean baseline before injecting a fault, use one preset at a time, and reset faults after the review. Preset JSON files contain the answer key.
