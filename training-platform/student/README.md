# Student case controls

These controls are intentionally student-facing. Running and modifying cases is part of the exercise; memorizing PowerShell is not. Start the week and pass its smoke test before applying a case. Read the case JSON, state your expected result, run it, collect evidence, then reset it. Custom cases belong in `cases/custom/` and should include a learning goal and expected signal.
